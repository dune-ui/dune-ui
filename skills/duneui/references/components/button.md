<!-- GENERATED FILE — do not edit by hand. Regenerated by the SkillsGenerator from
     src/DuneUI/TagHelpers/Button/*.cs and docs/DocsSamples/Pages/Button/*.cshtml.
     (Phase 1: hand-authored as the template the generator must reproduce.) -->
---
component: Button
tags: [dui-button]
generated: true
---

# Button

The button variant.

**Tag:** `<dui-button>` — renders a native `<button>` with `data-slot="button"`.

## Attributes

| Attribute | Type            | Default   | Allowed values |
|-----------|-----------------|-----------|----------------|
| `variant` | `ButtonVariant` | `Default` | `Default`, `Destructive`, `Outline`, `Secondary`, `Ghost`, `Link` |
| `size`    | `ButtonSize`    | `Default` | `Default`, `ExtraSmall`, `Small`, `Large`, `Icon`, `IconExtraSmall`, `IconSmall`, `IconLarge` |
| `class`   | `string`        | —         | Extra Tailwind utilities; merged last, so it overrides defaults. |

> In Razor, enum values are written fully-qualified, e.g. `variant="ButtonVariant.Outline"`.

## Examples

**Basic usage** (buttons with an icon and a default primary button)

```razor
<dui-button variant="ButtonVariant.Outline">
    <dui-icon name="undo-2"/>
    Cancel Changes
</dui-button>
<dui-button>Save Changes</dui-button>
```

**Variants** (across one size)

```razor
<dui-button>Default</dui-button>
<dui-button variant="ButtonVariant.Secondary">Secondary</dui-button>
<dui-button variant="ButtonVariant.Outline">Outline</dui-button>
<dui-button variant="ButtonVariant.Ghost">Ghost</dui-button>
<dui-button variant="ButtonVariant.Destructive">Destructive</dui-button>
<dui-button variant="ButtonVariant.Link">Link</dui-button>
```

**Icon + label at a given size**

```razor
<dui-button size="ButtonSize.Small" variant="ButtonVariant.Secondary">
    <dui-icon name="circle-arrow-left"/>
    Secondary
</dui-button>
```

## Related

- `dui-linkbutton` — an anchor styled as a button (same variant/size options).
